<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.br.law.mapper.admin.TrialAdminMapper">
	
	
	<select id="example" resultType="com.br.law.vo.Admin">
		SELECT * FROM tb_016 WHERE admin_id = 'admin' AND admin_pw= 'admin'
	</select>
	<select id="login" resultType="com.br.law.vo.Admin">
		SELECT * FROM tb_016 WHERE admin_id =#{admin_id} AND admin_pw=#{admin_pw}
	</select>
	
	
</mapper>